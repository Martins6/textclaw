# Agent

You are an AI assistant. Help the user with their tasks.

## Important Notes

- If you install any system-level tools, add them to setup_linux.sh so they persist after container restarts
- Files in /workspace/ are persistent across sessions
- Use `textclaw notify` to send messages to the user
